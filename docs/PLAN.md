# BallastViewer — Roadmap

Rebuild of BALLASTVIEW per `docs/BALLASTVIEW-SPEC.md`. One step per Claude Code session; each step ends buildable and testable. Check off steps as they complete and add notes future steps need.

## Fixed decisions

- Libraries exceed **50,000 photos** — speed is the top priority.
- **No compatibility** with old `MyLibrary.json` files. Fresh schema, no migration, no magic UUIDs from spec §7.5.
- **Persistence:** GRDB 7 / SQLite (WAL) inside a `Name.ballastlib` document package. In-memory catalog on the MainActor is the hot-path authority; DB is write-through (single-row updates). Collection counts update incrementally: O(changed photos × collections), full recompute only on bulk changes.
- **Keywords by id:** one `keyword` table (parent_id tree, nullable group_id; group_id = NULL ⇒ ad-hoc/grey). `photo_keyword` join table. Rename = one UPDATE (fixes C4). Paths (`PEOPLE > ANNA`) are derived.
- **Thumbnails:** ImageIO downsampling into buckets (256/768/2048 long edge), decode unrotated (Q5), rotate in the view layer (cache never invalidated by rotation). NSCache (~300 MB) + disk cache in Caches (HEIC), bounded decode concurrency, cancellation.
- **Actions:** one `AppAction` enum + `ActionDispatcher` (@MainActor) shared by keyboard/menus/MIDI. Exhaustive switch (fixes C5). `ControlSurfaceState` broadcast prepares LED feedback long before MIDI lands.
- ~~**Sandbox on**, security-scoped bookmarks at folder scope. USB entitlement added only in step 11.~~ **Superseded by U42 (2026-08-30): sandbox removed.**
- All spec §16.1/16.2 bugs (D1–D6, C1–C14) get fixed; §16.3 quirks (Q1–Q28) preserved unless a deviation below overrides.

## Deliberate deviations from the spec (U-numbers)

| # | Change |
|---|---|
| U1 | Onboarding: empty state with *Create Library…* / *Open Library…* buttons + folder drop target; New Library flows straight into Add Folder. |
| U2 | Import is recursive by default with an "Include subfolders" toggle (fixes C1); drag & drop import; progress + summary ("312 added, 40 skipped"). |
| U3 | Batch semantics win (C12): rating/rotation/keyword toggles apply to the whole selection from every source; anchor drives display/LEDs only. |
| U4 | Mixed-rating multi-selection shows a "mixed" indicator, not 0 stars. |
| U5 | Search matches keywords AND filenames (fixes C6). *(Post-step-12 user requests: the floating "active filter" chip is gone, and the search field + "n / m" position label are gone from single mode — the bar there is just the mode switch. The filter itself stays active across mode changes.)* **User decision 2026-08-23:** U5 keeps only part of C6 — the search field is hidden in single mode, but the filter stays active there and is surfaced as an "Active search: … (n/m)" label; it is not re-shown as an editable field. |
| U6 | ~~Grid badges~~ *(removed entirely post-step-12 on user request: first the rating pips, then the keyword dots and the View-menu toggle; thumbnails are now unadorned).* |
| U7 | Confirmations for all destructive actions with affected counts (fixes C14). |
| U8 | Undo (NSUndoManager) for photo mutations incl. batches, and folder removal. |
| U9 | Sort options: Filename (real filename), Path (original behavior), Capture Date (new — EXIF DateTimeOriginal in the model), Date Added, Random (Q2 intact). **Rating sort deliberately dropped:** comparators never depend on mutable culling fields — a Rating sort would reorder the list under the user's hands on every rating press and fight the Q1 neighbour rule. |
| U10 | Rule editor: "Unrated" star position, Capture Date rule, missing operators implemented, hint banner on empty rule list. |
| U11 | Shortcut conflicts shown inline ("was: Next Photo") instead of silent stealing. |
| U12 | MIDI: hot-plug (C10), length-based parser (C9), output device picker, distinct sync alert wording. |
| U13 | Menu stays "Library"; library shown by filename with extension consistently. *(Partly superseded by U27: a user-set display name wins; the filename with extension remains the fallback.)* |
| U14 | Library menu is a pure switcher (all known libraries, checkmark on the open one, ⇧⌘L Manage Libraries…). ALL management lives in Settings ▸ Libraries: a select field picks the library to manage (never opens it), its folders listed below with add/remove — imports work into closed libraries via a short-lived pool — plus New / Add Existing (validated, not opened) / Delete (red, confirmed, to Trash). Known-libraries list is unbounded (replaces the 10-entry recents); the fixed ⇧⌘N/⇧⌘O/⇧⌘I shortcuts are gone. Day-to-day import stays via drag & drop and the sidebar button. |
| U15 | Single view decodes the ORIGINAL file (memory-cached, no disk cache) instead of a 2048 px thumbnail; the 2048 bucket is gone entirely (buckets are now 256/512/768, grid-only — `ThumbnailBuckets.all`). User-driven: libraries hold modest-sized images, no RAW — **RAW extensions (raw/cr2/nef/arw/dng) are no longer accepted by `FolderScanner`** (supersedes the spec §5.3 list). |
| U16 | **BallastPicker** utility (Library ▸ BallastPicker, ⇧⌘P): a standalone first-pass selection window, fully decoupled from the library (no DB, no key map, no MIDI). Choose a root; its direct sub-folders are listed left with photo count + green `_auswahl` count; right is single view only (no footer, no refresh; root chosen once per picker session via the empty-state button; no toolbar; folder panel collapses via a grab-handle at its edge; full screen is the native green-button/⌃⌘F one). Fixed keys while the picker window is key: ← → step, Space rotate (lossless, EXIF+XMP orientation via `MetadataWriter.writeOrientation`), Return moves the file into `<folder>/_auswahl` and advances, ⌫ moves the last pick back. `_auswahl` contents are never listed. Code: `PhotoPickerScanner` (Core), `PhotoPickerModel`, `Views/PhotoPicker/`, `PhotoPickerCommands`. Debug hook `BV_TEST_PICKER_ROOT=<dir>` opens it at launch. **Deviations from spec §1.4 ("never modifies files on disk"):** the picker MOVES files (into `_auswahl` and back on ⌫) and Space writes the orientation tag into the file (metadata-only, pixels untouched, `PixelInvariantTests`). **Guard:** a root or sub-folder that the OPEN library catalogs (a `folder` row, or a descendant of a recursive one — `LibraryController.isFolderInOpenLibrary`, injected into `PhotoPickerModel.isFolderInOpenLibrary`) is refused with a non-blocking inline notice ("This folder is part of the open library — BallastPicker moves files and would break the catalog.") and not loaded. |
| U17 | **Save/Load Metadata commands removed**; replaced by automatic Lightroom-compatible write-through of keywords (`dc:subject` + `lr:hierarchicalSubject`) and `xmp:Rating`, debounced 2 s per photo, flushed on photo change and at quit (`MetadataWriteThrough`). Orientation stays library-only (never written to files; spec §6.2 tiff:Orientation write is gone). Pixel invariant unchanged (`CGImageDestinationCopyImageSource`). Supersedes the step 10 "Library has both sync items" acceptance criterion and U12's "distinct sync alert wording". |
| U18 | Rule semantics: an EMPTY rule value means "unconfigured" and matches all photos; a collection whose rules are ALL of unknown type matches nothing (D6 skips unknown types, but a rule set that becomes empty through skipping is not "match everything"). |
| U19 | Date rules (Date Added / Capture Date) are day-granular: the value is stored as local midnight and compared by day. |
| U20 | Q2 + U8: undoing a rating change re-enters the photo into Random order at a NEW position (the stable random order is not restored by undo) — accepted. |
| U21 | Kept quirk: `keywordGroup` rules with `greaterThan`/`lessThan` are treated as the positive operator (spec §7.3 leaves them undefined). |
| U22 | Kept quirk: Move Down on the last, partial grid row does nothing (no wrap, no clamp to the last cell). |
| U23 | Known libraries / recents are unbounded (supersedes spec §15.1 "max 10"). |
| U24 | Key and MIDI keyword bindings store the resolved path (C7) and are rewritten when a keyword (or an ancestor) is renamed (`LibraryController.keywordPathRenamed` → `KeyMapStore`/`MidiMapStore.renameKeywordPath`). |
| U25 | MIDI LEDs: the Q3 re-assert after Note Off is sent only for pads that are lit; LED output skips IAC (virtual inter-application) endpoints to avoid feedback loops. |
| U26 | Settings ▸ Keywords shows an **UNGROUPED** pseudo-group LAST listing every top-level keyword without an effective group (ad-hoc keywords). It is not colourable, renamable, deletable, reorderable or a "+"/drop target; its keywords support the normal row actions (rename, add sub-keyword, delete with the U7 confirmation) plus "Move to Group" (context menu on any top-level keyword; `LibraryController.setKeywordGroup`, `KeywordTree.settingGroup`). Fixes the original's undeletable ad-hoc keywords. |

| U27 | **Library display name** (user request 2026-08-29): `libraryMeta.name` (migration `v5-library-name`), editable in Settings ▸ Libraries via a Name field under the picker (commit on Return/focus loss/selection switch; blank clears back to the filename; works for closed libraries via the short-lived pool). Shown everywhere the filename was: window title, Library menu, Settings picker/folder header/delete alert. A UserDefaults cache (`libraryDisplayNames`, path → name, maintained by `BookmarkStore`) lets closed libraries show their name without opening pools; refreshed from the meta row on every open and on Add Existing. The authority is each library's own `library.sqlite`. |
| U28 | **UNRATED sidebar row** (user request 2026-08-30): below the ★ row, `star.slash` icon, exact rating 0 — `SidebarItem.rating(0)` (codec range widened to 0…5); counts came free (`ratings[0]` was already tracked). |
| U29 | **Inspector keyword-tree section** (user request 2026-08-30): collapsible KEYWORDS section at the inspector's bottom (default collapsed; expansion + height persisted in UserDefaults, height default 30% of the panel, draggable via a horizontal PaneDivider twin). Full hierarchy with subtree-inclusive, per-photo-deduplicated counts (`KeywordCounts`, event-driven recompute in `KeywordPanelModel` — never per body pass, never while collapsed). Clicking a keyword filters GLOBALLY via `SidebarItem.keyword(id)` (photo carries the keyword or any descendant — `PhotoQueryFacts.keywordIds` ∩ precomputed subtree), replaces the sidebar selection and discards an active search. No colors, no View-menu toggle (mouse only, per user). |
| U30 | **Import Metadata from Lightroom** (user request 2026-08-30): Library ▸ "Import Metadata from Lightroom…" merges ratings + hierarchical keywords from a Lightroom Classic `.lrcat` (itself SQLite, read via GRDB from a temp copy incl. WAL sidecars) into photos ALREADY in the open library — never adds photos. Matching: exact path (folded + NFC) first, then unique-filename fallback for moved files; anything ambiguous is skipped, never guessed. Merge is additive (keywords union; LR rating wins only where LR has one; nil = leave alone), one pipeline transaction (`LightroomImportDAO.merge`), no-op-aware so a rerun re-flags nothing, `needsFileWrite` set in the same txn, then write-through as usual. Summary alert reports matched/by-path/by-filename/applied/unmatched/ambiguous. Not undoable (bulk, like folder import). Core: `LightroomCatalogReader`, `LightroomMatcher`, `LightroomImportDAO`. Ambiguous candidates get a dated review keyword ("LIGHTROOM IMPORT dd.MM.yyyy", top-level ad-hoc, written through to files like any keyword) + a same-named smart collection (`keyword equals`, group "LIGHTROOM", both find-or-create/idempotent) via `markIssues` in the same transaction — deleting the keyword cleans up fully. Sidebar bottom gains `FileWriteStatusSection` (KEYWORDS-section style, default expanded): visible ONLY during a declared bulk run — `MetadataWriteThrough.isBulkRun`, set by the import (and by a ≥25-photo backlog re-queued on open after an interrupted run), cleared on drain (kept while run failures are open); everyday single-photo writes never show it. Progress bar over the run (`MetadataWriteThrough.runTotal`/`completedCount`, in-flight counted) + failed-files count. (+16 tests) |
| U31 | **Check for Updates** (user request 2026-08-30): BallastViewer ▸ Check for Updates… (`AppUpdater`, menu next to About). Fetches the latest GitHub release (repo made PUBLIC for this — the updater has no token; do not flip it private), compares `v0.1.<commit count>` against `CFBundleVersion`, on confirm downloads the zip into the container (no quarantine → no Gatekeeper friction despite ad-hoc signing), unpacks via ditto, trashes the running bundle, moves the new one into place, relaunches (`createsNewApplicationInstance`). Sandbox: needs write access to the bundle's parent — asked ONCE via NSOpenPanel, kept as security-scoped bookmark; later updates run fully automatic. New `network.client` entitlement. Feed parsing/version arithmetic in `UpdateFeed` (BallastCore, tested); E2E-verified by downgrading a build to 0.1.1 and letting it self-update to v0.1.94. **Hardening after a real-world half-install (2026-08-30):** determinate download progress overlay in the main window (`downloadProgress`, streamed via URLSession.bytes); three-step install — stage the new bundle INTO the destination folder first, then trash the old (keeping its trash URL), then rename, with FULL ROLLBACK (old app put back) if any later step fails, so "old app gone, new app missing" can never happen again; every modal (alerts, grant panel) is preceded by `NSApp.activate` — an invisible behind-window panel once made the app look frozen and unquittable. **Relaunch fix (2026-08-30, real-world dual-instance report):** quit FIRST, start SECOND — a detached shell waits for this PID to vanish (write-drain included) and then opens the updated bundle; and terminate is issued via `RunLoop.main.perform`, never from a dispatch block, because `_shouldTerminate`'s nested event loop cannot drain the main queue from inside a queue drain — the AppDelegate drain task never replied and the old instance stayed open forever. |
| U32 | **Collections list alphabetically** (user request 2026-08-30): smart collections inside a group sort case-insensitively by name (fold, presentation-level in `SidebarViewModel.refreshLists`) instead of by creation/sortOrder; the collection drag-reorder is gone (it fought the fixed order — `LibraryController.reorderCollections` removed, `CollectionDAO.reorderCollections` + the `sortOrder` column stay in core untouched). Groups keep their drag order. |
| U33 | **⌘A Select All** (user request 2026-08-30): new `AppAction.selectAll`, default binding `cmd+a` (remappable in Settings ▸ Shortcuts like everything else; spec §12.2/§12.3 counts grew to 20/17). Grid-only (`CenterViewModel.selectAllVisible` — an invisible mass-selection must not sit behind the single view and swallow a rating press); the anchor stays put, falls back to the first photo. Q21 keeps working: while a text field is focused the monitor passes ⌘A on, so it selects text, not photos. Stored key maps predate the binding — `BindingMapStore.adoptDefaultOnce` merges it exactly once, only if key and action are both unbound (a remap or deliberate unbind is never fought). No menu item: the standard Edit ▸ Select All stays the text-field one. |
| U34 | **Draft pattern for vocabulary creation** (user bug report 2026-08-30, supersedes the spec §8.6 create-then-edit quirk): Settings ▸ Keywords "New Group" opens the sheet on a DRAFT (nil id, next free palette colour) and creates only on Save; the keyword "+" (group +, sub-keyword +, context menu) opens a "New Keyword" alert with an empty name field and creates only on Create (empty name = no-op). Cancel leaves no trace in either flow. Renaming/editing existing nodes unchanged. |
| U35 | **Move nested keywords to a group's top level** (user request 2026-08-30, the sorting step after a Lightroom import): "Move to Group" in the Settings ▸ Keywords context menu now also appears on NESTED keywords — the keyword is lifted to the top level of the chosen group (or UNGROUPED), subtree included. A same-named top-level keyword ABSORBS the moved one: photo assignments union, same-named children merge recursively, the source node is deleted (`KeywordDAO.moveToTopLevel`/`merge`). Carriers of the subtree get new derived paths → facts invalidated, `.photosUpdated`, `needsFileWrite` + write-through. In-memory tree AND join table reload from the transaction (merge makes deltas unreliable). NOT undoable (a merge has no clean inverse). The emptied Lightroom parent stays for manual deletion. Top-level keywords keep the existing group-only semantics (U26). |
| U36 | **Explicit "Create" row in the keyword dropdown** (user request 2026-08-30): same-named keywords under different parents are first-class in the model, but the Q16 bare-name resolution made a new top-level "2008" unreachable from the Add Keyword field while any "2008" existed. The autocomplete dropdown now appends a `+ Create "X"` row whenever the input, read as an EXACT path, does not exist (`KeywordAutocomplete.createOption`); picking it (click or ↓/Return — highlight indexes run over suggestions then the create row) creates and assigns exactly that path via `LibraryController.assignKeyword(exactPath:)`, bypassing Q16. Plain Return keeps the Q16 first-match behaviour untouched. Inspector field only; the search field is unchanged. |
| U37 | **OR search** (user request 2026-08-30): whitespace splits the search query into OR terms — "PARIS ROME" shows photos matching PARIS or ROME; each term still matches keywords AND filenames (U5). A `>` token glues its neighbours into ONE path term, so "LOCATION > ROME PARIS" is "LOCATION > ROME" OR "PARIS" (a stray ">" never becomes a match-everything term). `SearchFilter.FoldedQuery.terms`; single-term queries behave exactly as before. |
| U38 | **Rating chips next to the search field** (user request 2026-08-30): star.slash + ★1–5 right of the grid search field. Click = exact-rating filter (Q9 semantics like the sidebar rows; cumulative star DISPLAY, filter 4 fills ★1–4), second click on the active value turns it off; one value at a time. ANDed with the text search and the active collection (`CenterViewModel.ratingFilter`, applied without debounce). Survives mode switches like the search (U5) and shows in the single-mode "Active search: …" label ("ROME · ★4"); the U29 keyword-tree click discards it together with the search. |
| U39 | **Focus shortcuts** (user request 2026-08-30): "s" focuses the search field, "k" the inspector\'s Add Keyword field — default bindings, remappable like everything (spec §12.2/§12.3 counts now 22/19; adopted once into stored maps). Dispatcher-side the actions also get the field on screen first: "s" switches single → grid (U5), "k" reveals the right panel. Focus itself travels as a monotonically increasing request token in CenterViewModel with CENTRALISED consume-once (`consumeSearchFocusRequest`/`consumeKeywordFocusRequest`) — the field takes it via onChange when live and via onAppear when the same action just created it, and a stale token can never steal focus on an unrelated re-appearance. Q21 keeps both keys typing normally inside any focused text field. |
| U40 | **Rename-collision merge** (user request 2026-08-30): renaming a keyword to a name an existing SIBLING carries ("_STRASSE" → "STRASSE" under META) no longer just errors — the Settings editor offers to MERGE instead: a confirmation alert states the blast radius (sub-keyword and photo counts, "cannot be undone"), and on Merge the sibling absorbs the renamed node with the U35 semantics (assignments union, same-named children merge recursively, source deleted; `KeywordDAO.merge` is now public with a cycle guard, `LibraryController.mergeKeyword`). Carriers get facts invalidated, `.photosUpdated`, `needsFileWrite`; key/MIDI bindings on the old path re-point at the survivor. Renames without a collision are unchanged, and the plain error remains for non-interactive callers of `renameKeyword`. |
| U41 | **Child Smart Collections** (user request 2026-08-30): a collection can nest under another — its result is the parent's result AND its own rules, every level keeping its own All/Any mode (`CompiledRuleChain.chains`; counts and the active filter evaluate the chain). Sidebar renders an outline — a fixed chevron column on every row (invisible on leaves, so siblings align) plus one column of indent per ancestor level (user decision 2026-08-30: indent + chevron only, guide lines tried and removed); alphabetical per level (U32), disclosure state transient. Created via context menu: "New Smart Collection" on the group header (the header's "+" is gone — creation is context-menu-only now, superseding the Q25 hide-on-collapse detail), "New Child Collection" on a collection (child lives in the parent's group; `groupId` mirrors the parent's). The editor shows inherited ancestor rules greyed/read-only with their origin ("from X") and warns when edits propagate into N children. Deleting a parent deletes the subtree (v6 migration: `smartCollection.parentId`, FK cascade) after a U7 alert naming the child count. Rule-less child = exactly the parent's result (Q6 analogue). Cycles/dangling parents in hand-edited libraries are cut defensively. |
| U42 | **App Sandbox removed** (user decision 2026-08-30, after the first real-world update bricked the install): a sandboxed app quarantines EVERY file it writes (`0086`), Gatekeeper refuses to exec such bundles outright ("File created by an AppSandbox, exec/open not allowed") and the sandbox forbids removing the attribute — the U31 self-updater was structurally impossible. Entitlements file deleted; new bookmarks are PLAIN (`PortableBookmark`, still move/rename-tracking) with a read fallback for pre-U42 security-scoped blobs (library list, last-opened, folder bookmarks in library.sqlite all keep working); `startAccessingSecurityScopedResource` stays as a harmless no-op on plain URLs. The updater's NSOpenPanel folder-grant + stored bookmark are gone — parent-folder writability is checked directly. `SandboxMigration` imports the container's UserDefaults once (marker-guarded, never clobbers newer keys). Trade-off accepted: no exploit containment for crafted images, no MAS path. **Transition caveat:** a still-sandboxed install (≤ v0.1.116) updating in-app quarantines the incoming bundle one last time — fix once per machine via fresh browser download + right-click-open, or `xattr -dr com.apple.quarantine`. E2E: downgraded 0.1.1 copy self-updated to v0.1.116, relaunched clean, no quarantine; container prefs + old bookmarks migrated. |
| U43 | **Incremental Lightroom imports** (user request 2026-08-30): a rerun of the U30 import used to re-create keyword paths the user had reorganized since ("JAHRE > 2009" came back next to the moved "2009") — because merged photos were re-matched every time. Now the merge stamps every matched photo `lightroomMergedAt` (v7 migration; no-op matches too, in the same transaction), and later runs drop stamped photos from the match set BEFORE merging — so folder-by-folder import + reorganize between runs works. Matching itself still sees ALL photos (filtering the input would let a stale catalog entry filename-match a same-named new photo the old photo used to make ambiguous); only the resulting matches are filtered. When every match is stamped (catalog covers nothing new), an alert offers a deliberate full re-import — e.g. after rating more photos in Lightroom — which re-applies and re-stamps everything; headless hook runs auto-confirm. Summary reports "N photos from an earlier import were left untouched." Ambiguous photos are never stamped (they were never merged; better data later may resolve them). **Caveat:** libraries enriched before v7 carry no stamps, so their next import touches old photos one last time. (+2 tests) |
| U44 | **Single-view footer names the active filter** (user request 2026-08-30): the U38 "Active search: …" label only appeared for search text / rating chips — the sidebar selection stayed invisible in single view. The footer now ALWAYS shows the full filter chain: the active sidebar item first (`SidebarItem.displayName` in Core — sidebar wording: ALL PHOTOS / LAST IMPORT / UNRATED / ★-repeated star rows; collections as their root-first ancestor chain "TRIPS > 2009"; U29 keyword-tree picks as the keyword path), then search text and U38 rating chip where set, " · "-joined, with the existing (position/count) suffix. Cycle-guarded parent walk, "?" fallback for ids a hand-edited DB no longer resolves. The "Active search:" prefix is gone — the parts speak for themselves. Always visible is deliberate (user confirmed 2026-08-30): even a plain "ALL PHOTOS (n/n)" stays — it doubles as the position indicator; do not hide it for the unfiltered case. (+5 tests) |
| U45 | **Duplicate Smart Collection** (user request 2026-08-30): context-menu "Duplicate" on a collection row creates a SIBLING copy — same group, same parent, same All/Any mode, rules copied (`CollectionDAO.duplicateCollection`) — named "NAME (COPY)", or "NAME (COPY n)" when taken among that sibling level (same group AND parent). Children are deliberately NOT cloned: the duplicate exists to tweak rules, not to clone subtrees. The copy is a plain collection afterwards — edit/delete/nest as usual. (+3 tests) |

## Steps

- [x] **1. Scaffolding (S)** — XcodeGen + `project.yml`, sandboxed app skeleton (3-pane HSplitView, 800×600 min), `BallastCore` SPM package with GRDB dep + first test, CLAUDE.md, this file, spec moved to docs/, .gitignore, scripts/build.sh + test.sh, git init.
  *Acceptance:* `./scripts/build.sh` succeeds, app launches showing empty state, `./scripts/test.sh` green.
- [x] **2. Domain + database (M)** — Schema v1 (photo, folder, import_batch, keyword tree, photo_keyword, keyword_group, smart_group/collection/rule, library_meta), `DatabaseMigrator`, DAOs, `.ballastlib` create/open, in-memory catalog load, six default keyword groups (spec §8.3 colors).
  *Acceptance:* DAO/migration tests green in-memory incl. rename propagation (C4) and cascade rules; 50k synthetic rows insert + full load < 1 s.
- [x] **3. Library lifecycle (M)** — New/Open/Recent (max 10, deduped)/Close, security-scoped bookmarks, auto-reopen at launch, onboarding (U1), Library menu with ⇧⌘N/⇧⌘O. Errors surfaced as alerts, never silent (spec §4.1/4.2 fixes).
  *Acceptance:* create → relaunch auto-opens; Close prevents auto-reopen but keeps recents; corrupt file shows alert.
- [x] **4. Import + metadata read (M)** — Recursive scan with toggle (C1), ImageIO reader: XMP first, IPTC fallback (D1 read half), rating clamped 0–5 (D5), capture date, uppercase/dedupe/sort keywords; one batch id per invocation (⇧⌘I); Remove Folder with path-component matching (D4) + confirmation (U7); drag & drop (U2); bounded concurrency (12); extensions per spec §5.3.
  *Acceptance:* nested folders import fully; re-import dedupes by path; empty rescan keeps LAST IMPORT (Q7); `/Photos/Trip` removal spares `/Photos/Trip2024` (test).
- [x] **5. Grid + thumbnail pipeline (L)** — ThumbnailPipeline actor, disk+memory cache, grid (columns 1–10, square cells, fill-crop top-aligned Q22, spacing-as-padding Q23, 3 pt accent border), mouse selection (click/shift/cmd/double-click per spec §9.3), `SelectionModel` in BallastCore.
  *Acceptance — PERF GATE:* 50k synthetic photos scroll smoothly, selection < 16 ms, memory bounded, disk-cache hits skip decode. If LazyVGrid fails the gate, switch to NSCollectionView backend behind the grid protocol *within this step*.
- [x] **6. Culling core (L)** — `AppAction` + `ActionDispatcher` (all 19 actions, C5), AppKit key monitor + default map (spec §12.3, grammar §12.1), batch semantics (U3), neighbour rule Q1, stable random Q2, sort engine (U9), rotation Q5, single view with downsampled decode + `n / total`, bottom bar, Photo/View menus with live-updating shortcuts, `ControlSurfaceState` broadcast (MIDI prep).
  *Acceptance:* NeighbourRule/StableRandom/sort/key-grammar unit tests green; live: in an "unrated" collection, rating advances to next unrated photo, never to the top; shortcuts work after mode/collection switches without clicking.
- [x] **7. Sidebar + query engine + smart collections (L)** — Rule matrix spec §7.3 as table-driven tests, keywordGroup matches resolved paths (C2), unknown rules skipped (D6), pseudo-collections (ALL PHOTOS / LAST IMPORT / exact-match star rows Q9, empty-before-first-import Q8 via real match-nothing), incremental `CollectionCountsStore`, groups (drag reorder, collapse persisted in DB), rule editor sheet (U10, edits a copy), confirmations (U7).
  *Acceptance:* §7.3 matrix tests green incl. nested-keyword group rule; rating a photo updates ★ badges instantly via delta only; collection switch auto-selects first photo.
- [x] **8. Keywords + inspector (L)** — Vocabulary editor (Settings ▸ Keywords), ADD KEYWORD field (forced uppercase Q15, path resolution first-match Q16, autocomplete over all nodes Q17 with ↓ wrap/↑ semantics per spec §9.8), chips (group color/order Q18, intersection display Q14), rating stars (tap-current-clears Q12, mixed indicator U4), Reveal in Finder + Share.
  *Acceptance:* typing `ANNA` assigns `PEOPLE > ANNA` when the node exists; renaming a node updates chips everywhere instantly (C4); chip order = group order then alpha, ungrouped grey last; arrow keys never leak to the grid.
- [x] **9. Search + settings + shortcut editor (M)** — Search with visible filter chip in both modes (C6/U5), keyword+filename matching, suppression while focused (Q21); Settings window (Appearance / Keywords / Shortcuts); key recorder with conflict hint (U11); keyword shortcuts through the same normalize-and-resolve pipeline (C7); canonical `#RRGGBBAA` colors (C8); Reset Defaults restores spec §12.3 map.
  *Acceptance:* active search survives mode switch visibly; no shortcut fires while search focused; binding updates menus live; keyword shortcut `anna` toggles `PEOPLE > ANNA`.
- [x] **10. Metadata write-back + sync + undo (M)** — Lossless XMP write (tiff:Orientation, xmp:Rating, dc:subject; merge mode; temp file + atomic replace, spec §6.2), ~~Save/Load Metadata commands with clear distinct wording (U12) + unreadable-file list (Q27 visibility)~~ *(superseded by U17 automatic write-through)*, undo layer (U8).
  *Acceptance:* pixel bytes identical before/after write; rating survives Save→Load round trip (D1); unassigned vocabulary survives Load (D2); Cmd-Z reverts a batch as one step; `kill -9` during writes never corrupts the library (WAL).
- [x] **11. MIDI (L)** *(hardware checklist with a real pad controller still open — see step note)* — CoreMIDI client with hot-plug (C10), length-based parser in BallastCore (C9, unit-tested incl. phantom-note regression), velocity-0-as-off, `note:<0-based ch>:<n>` grammar / `CH<1-based> N:<n>` display, per-action debounce, LED protocol: on = NoteOn vel 127, off = NoteOn vel 0, cumulative stars (Q4), rate-0 exact, view-mode pads, **re-assert on Note Off (Q3)**, output device picker (default: all destinations), MIDI recorder in Settings (Return clears), USB entitlement added.
  *Acceptance:* parser + LEDStateComputer suites green; hardware checklist from spec §18 MIDI block passes with a real pad controller — released pads stay lit, hot-plug works.
- [x] **12. Polish + acceptance sweep (M)** *(open: hardware-MIDI checklist, Instruments with real photos — see docs/ACCEPTANCE.md)* — Grid badges (U6), app icon, thin overlay scrollbars (spec §9.10 look), remaining confirmations, `docs/ACCEPTANCE.md` = full spec §18 checklist with every line checked or annotated with its U-number, Instruments profiling with 50k real photos.
  *Acceptance:* keystroke→UI < 16 ms and keystroke→durable < 100 ms at 50k photos; no main-thread I/O in Instruments.

Dependencies: 1→2→3→4→5→6→7→8; 9 and 10 any order after 8; 11 needs only 6; 12 last.

## Step notes

- **U14 library management redesign (done, user-driven):** see the U14 row. Implementation notes: `BookmarkStore` recents became the unbounded known-libraries list (same defaults key, old entries carry over); `LibraryController.knownLibraries` + `addKnownLibrary`/`deleteLibrary(at:)` (trashItem behind the entry's security scope). Import refactored to `runImport(_:recursive:pool:)` with `importFolders(_:recursive:into:)` — the open library keeps the live-pool path (snapshot/events), closed targets get a short-lived scoped pool; `withManagedPool(at:)` backs `folders(inLibraryAt:)`/`folderPhotoCount`/`removeFolder(fromLibraryAt:)` (no undo for closed-library edits, confirmation only). `SettingsRouter` lets "Manage Libraries…" deep-link the Settings tab via `@Environment(\.openSettings)` (works inside Commands). Settings select field is management-only state (`selectedPath`), deliberately NOT bound to the active library. Verified: BVMANAGE hook — import into a CLOSED library (folders 1→2, photos 1→9) while another stayed open and untouched; 119 core tests.
- **MIDI hardware verification (done, Intech Grid):** full §18 MIDI block user-verified — cumulative star meter (Q4), rate-0 exact, view-mode pad, per-photo re-light on navigation, **released pads stay lit (Q3)**, USB hot-plug reconnect + re-light (C10). Two real bugs surfaced during testing: (a) the Grid is a **composite USB device (MIDI + CDC serial + HID keyboard class)** — with scroll bars on "automatic", plugging/unplugging it makes macOS flip the system scroller style, which RESETS our explicit `.overlay` style and lets the legacy bar eat grid width; fixed by re-asserting `.overlay` on `NSScroller.preferredScrollerStyleDidChangeNotification` (+ in updateNSView). (b) That width flip exposed a knife-edge in `GridFlowLayout`: cells computed to EXACTLY fill the width sometimes wrap a row early and the flow layout justifies the leftovers into huge gaps ("spacing changed") — fixed by flooring the cell size. Diagnosis path worth remembering: virtual endpoints reproduce setup-changed but NOT the HID side; `hidutil list` / `ioreg` revealed the composite interfaces.
- **Hardening pass 2 (done):** `WindowGroup` → single `Window` scene — File ▸ New Window would have shown a second view of the shared controller/selection and stolen the shortcut whitelist. The Window scene drops the automatic File menu entirely, so a `CommandGroup(replacing: .saveItem)` restores ⌘W Close (also closes Settings). `AppDelegate.applicationShouldTerminate` (`.terminateLater`) drains the WritePipeline via `LibraryController.drainWrites()` before exit — a rating pressed right before ⌘Q can no longer race process death. Verified: File menu is exactly [Close]; rate→⌘Q commits and terminates cleanly; BVCULL + 119 core tests green under the Window scene.
- **Hardening pass (post-step-12, done):** Findings from a full-codebase review, all verified against the existing hooks/tests. (1) **Write ordering:** unstructured `Task { pool.write }` per mutation had no ordering guarantee — `WritePipeline` (AsyncStream + one worker) commits strictly in submission order; `releaseCurrent` drains it before closing the pool (close+instant-reopen may briefly read pre-drain state; WAL-safe). `writeSync` (structural edits) intentionally bypasses the pipeline — commutative with it except contrived toggle-vs-vocabulary-delete races. (2) **Facts cache:** `LibraryController.queryFacts(forPhotoId:)` memoises `PhotoQueryFacts`; invalidated per-photo on assignment changes/metadata load, wholesale on vocabulary changes and snapshot reloads. Counts rebuild warms it, so search/filters/metadata sync hit a warm cache. (3) **Search debounce (150 ms, small spec deviation):** one refilter costs ~185 ms at 50k — clearing applies instantly, typing debounces; `applySearchNow()` for hooks/probe. (4) Thumbnails: mtime stat moved off-actor (it serialised every request), disk cache pruned to 2 GB oldest-first on open, redundant `fileExists` dropped. (5) **ShortcutMonitor whitelist:** MainWindow registers its NSWindow (`MainWindowRegistrar`); shortcuts fire only there — no more identifier sniffing (verified: key in Settings does nothing, main window still rates). (6) Group-color edits emit `.photosUpdated(carriers)` so badge dots refresh live. (7) Schema `v2-fk-indexes` (photo.folderId, photo.importBatchId) + migration tests. (8) `levelsOfUndo = 50`. **Headless-undo learnings (cost a lot of debugging):** NSUndoManager closes implicit groups at END OF EVENT — headless runs have no events, so gestures merge into ONE group (a single undo unwound the whole BVS10 flow to import state; LIFO composite). Fix: `registerUndo` wraps each gesture in explicit begin/endUndoGrouping (inert interactively) and TestHooks sets `groupsByEvent = false`. Also: presenting SwiftUI alerts during hook runloop-spins crashes AppKit's sheet-close animation observer (flaky EXC_BAD_ACCESS) — `TestHooks.suppressesAlerts` now gates the MainWindow alerts. Perf after hardening (50k): select 9.1 / jump 11.0 / rateUI 6.5 / durable 6.6 ms, one search apply 185 ms, 121 MB. 119 core tests; BVCULL/BVKEY/BVS9/BVS10 green (BVS10 3×).
- **Step 12 (done):** U6 badges — `GridPhoto` now carries `rating` + `badgeColors` (effective-group colors in group order, grey last, capped 5; visibility is a view concern, toggled via View ▸ Show/Hide Badges, persisted `showGridBadges`); rendered as CALayer pips (white, bottom-left) and dots (bottom-right) in `GridItemView`, siblings of the image layer so the rotation transform doesn't touch them. Group-COLOR edits don't emit photo events (step-8 design), so badge dots refresh on the next photos/collection event, not instantly — acceptable. Q22 top-aligned crop via `contentsRect` (gravity `.resize`; unflipped layers put the bitmap's top row at unit y=1; per-rotation source rects, mirrored EXIF falls back to center) — verified visually with a red-top/blue-bottom portrait incl. the 180° case. Overlay `.mini` scroller on the grid (§9.10); sidebar/inspector keep system SwiftUI scrollers. Panel visibility + badges persist (defaults keys `showLeftPanel`/`showRightPanel`/`showBottomPanel`/`showGridBadges`). App icon: generated `App/Resources/AppIcon.icns` (script pattern in scratchpad; culling grid + star + LED pads), `CFBundleIconFile` via project.yml. **Perf gate at 50k re-run and FIXED:** rating a photo took 66 ms — two O(50k)-per-keystroke taxes: `photosDidChange` rebuilt the id→index dictionary each call (now a maintained `visibleIndexById`, also used by moveAnchor/anchorPosition instead of `firstIndex`), and the grid Coordinator full-compared 50k `GridPhoto` structs (now: cheap id sweep first; on the value path only realized items are struct-compared). Results: select 9.0 ms / jump 12.3 ms / **rate keystroke→UI 7.4 ms / keystroke→durable 7.5 ms** / 114 MB; PerfProbe gained the rate probes (durable = empty write queued behind the pending UPDATE; note `BV_TEST_QUIT` exits before PerfProbe runs — launch without it and kill). BVCULL regression green, 117 core tests. `docs/ACCEPTANCE.md` = full §18 sweep with per-line evidence; open there: hardware-MIDI block on a real controller, Instruments with real photos.
- **Step 11 (done, software-verified):** Core `Actions/Midi.swift`: `MidiAddress` (`note:<0-based ch>:<n>` ↔ `CH<1-based> N:<n>`), `MidiMap` (1:1 like KeyMap, no defaults), `MidiParser` (length-based C9 — CC/pitch/aftertouch skipped whole, SysEx consumed through 0xF7, vel-0 → off, truncated tail dropped; phantom-note regression test), `LEDStateComputer` (§13.6: keyword exact, rate0 exact-zero, stars cumulative Q4, viewGrid/viewSingle/toggleViewMode, everything else dark). App: `MidiMapStore` (defaults `midiShortcuts`), `MidiService` — **three hard-won platform learnings:** (1) legacy `MIDIInputPortCreateWithBlock` receives NOTHING on current macOS — use `MIDIInputPortCreateWithProtocol(._1_0)` and flatten MIDI-1.0-in-UMP words (mt==2; program/channel-pressure emit 2 bytes) back into the byte stream for the core parser; (2) **CoreMIDI callbacks must be explicitly `@Sendable` typed** — formed inside a @MainActor method they inherit main-actor isolation and the Swift 6 runtime assert (`dispatch_assert_queue_fail`, EXC_BREAKPOINT) kills the app on the FIRST incoming event; hop via `DispatchQueue.main.async` + `MainActor.assumeIsolated`; (3) legacy `MIDIReceived` on virtual sources is equally dead — `MIDIReceivedEventList`. Hot-plug (C10) via `.msgSetupChanged` → connect new sources + refresh destinations + full LED resend (a freshly plugged controller lights up immediately). Per-action debounce from `AppearanceStore.midiDebounceMs`; LED sends are diffs, plus Q3 re-assert of the single pad on every Note Off; output goes to all destinations or the one picked in Appearance ▸ "LED Output Device" (`midiOutputDestination`). Recorder: `MidiService.captureHandler` (§13.8) + `MidiRecorderView` (orange `MIDI…`, Return-while-armed clears); both recorders on every shortcut row, add-row needs name + at least one binding, Reset Defaults clears the MIDI map, U11 hints cover MIDI overwrites too. USB entitlement added. Verified: 117 core tests; end-to-end via `scripts/bvmiditester.swift` (virtual source+destination CLI, compile with `DEVELOPER_DIR=/Applications/Xcode.app swiftc -o bvmiditester scripts/bvmiditester.swift -framework CoreMIDI`; app hook `BV_TEST_MIDI=1` seeds notes 60–63): hot-plug pickup of a source created after launch, NoteOn→rate3, keyword toggle, CC+Note in one packet → exactly one action (C9), initial LED resend to the new device, diff updates, **Q3 re-assert received after Note Off**; recorder capture + 1:1 rebinding checked live in Settings. **Still open for a session with real hardware:** the spec §18 MIDI checklist on a physical pad controller (released pads stay lit, hot-plug of USB hardware, debounce against double-triggering pads). Build gotcha: changing the entitlements file between `xcodegen` and `xcodebuild` trips "Entitlements file was modified during the build" — delete `build/` and rebuild.
- **Step 10 (done):** Core: `Import/MetadataWriter` — lossless write via `CGImageDestinationCopyImageSource` (never recompresses; pixel-identity is unit-tested) with a merge-mode patch of exactly `tiff:Orientation`/`xmp:Rating`/`dc:subject`, temp file in `.itemReplacementDirectory` + `replaceItemAt` (atomic, same volume); **empty keyword list writes `kCFNull` to delete the tag** — otherwise stale keywords resurrect on the next Load. `MetadataReader.readIfReadable` distinguishes unreadable (nil) from readable-but-empty — the sync commands skip-and-report unreadable files (Q27) instead of letting an empty read wipe data. `MetadataSync.differs` compares the three attributes with keywords as a set (both directions). `PhotoDAO.setKeywords` replaces a photo's assignment set. App: `LibraryController+Sync` — Save = read→compare→write per photo in a bounded (8) task group; Load = bounded reads, then ONE `applyMetadataValues` call: single write tx, keyword strings via `ensurePath` (vocabulary only grows, D2), memory patch, `.photosUpdated`, and the inverse state registered as ONE undo step (undo/redo reuse the same applier). Distinct alert wording (U12) + capped bullet list of unreadable/failed files. **Folder security scope**: `install()` now resolves and starts every folder bookmark (`accessedFolderURLs`, stopped in `releaseCurrent`) — without this the sandbox blocks file reads AND metadata writes after relaunch. Undo (U8): controller uses the **window's UndoManager** (injected via `@Environment(\.undoManager)` in MainWindow.onAppear) so the standard Edit menu + ⌘Z work and text fields keep their own undo; `registerUndo(actionName:_:)` helper, one call = one gesture = one group (NSUndoManager groups per event). Undoable: rating batches (absolute-restore via `setRatings`), rotation (`setOrientations`), keyword assign/remove (mutually inverse), folder removal (captures folder row + photos with ids + photo_keyword pairs, reinserts verbatim), metadata Load. Vocabulary edits deliberately not undoable. Verified: 101 core tests; BVS10 headless ×4 (file round trip through the app path, load file-wins, D2, load/batch/keyword/folder undo); kill -9 during a 300-write rating churn → `PRAGMA integrity_check` ok, reopen clean (WAL); menus checked visually (Edit shows live "Undo Rating Change"; ~~Library has both sync items~~ — superseded by U17, the sync commands are gone). **Headless-testing gotcha:** undo groups only close when the runloop reaches its end-of-event checkpoint — hooks must `RunLoop.main.run(until:)` between gestures (`spinRunLoop`), plain `Task.sleep` is not reliable. Step 11 note: MIDI-triggered actions dispatch on the main queue per event, so per-gesture undo grouping holds there too.
- **Step 9 (done):** Core: `Query/SearchFilter` (blank query matches all; case-insensitive substring over filename AND derived keyword paths, U5), `Models/HexColor` (canonical `#RRGGBBAA` parse/format, C8 — `App/Views/ColorHex.swift` now bridges through it and adds `Color.canonicalHex` for writing), `KeywordResolver.canonicalText` (C7: keyword *bindings* store the resolved path — `anna` → `keyword:PEOPLE > ANNA`; dispatch re-resolves anyway, so both ends are safe). App: `CenterViewModel.searchText` ANDs after the sidebar filter in `passesFilter` and routes changes through `applyFilterChange()` (Q1 relocation); the bottom bar shows the search field in BOTH modes (C6/U5, compressible min 80/ideal 200 — fixed widths overflowed the 400 pt min centre pane and pushed the mode picker out) with the §11.3 keyword-autocomplete dropdown overlaying upward; the active filter renders as a dismissible chip floating topTrailing over the content (bar was too cramped for it in grid mode). **SwiftUI overlays DO render above the NSCollectionView** — verified, so chips/dropdowns over the grid are safe. `AppearanceStore` (UserDefaults `gridSpacing`/`backgroundColor`/`midiDebounceTime`): spacing feeds `GridFlowLayout` via a `PhotoGridView` param, background colors `centerPane`, `midiDebounceMs` is stored for step 11. Settings has all three tabs; `KeyRecorderView`+`KeyCaptureSession` (one live app-wide monitor, cancels prior session, consumes everything while recording), `ShortcutsSettingsView` (19 action rows, U11 inline orange "was: X" hint computed BEFORE `KeyMap.assign`, keyword rows + add row, Reset Defaults). `ShortcutMonitor` now also stands down for the Settings window (identifier contains "settings") and while `ShortcutMonitor.recorderActive`. **Gotcha:** the search field grabbed first responder at launch, silently disabling every shortcut via Q21 — `MainWindow.onAppear` clears `initialFirstResponder` + resigns text focus; keep this in mind when adding focusable controls to the main window. `ActionDispatcher` now dispatches `.keyword` via `toggleKeyword(text:)` on the whole selection (U3 wins over spec §12.7 anchor-only). New hook `BV_TEST_STEP9=1` (BVS9: canonical binding, toggle on/off, keyword/filename/miss search counts). Verified: 93 core tests; BVS9 all-pass; scripted UI run — Q21 (typed digits filter instead of rating), chip+field survive grid⇄single, U11 hint shows, ⌥⌘N recording updates the Photo menu instantly, Reset Defaults restores the exact §12.3 plist, appearance defaults render (40 px gaps, green background). Step 11 notes: MIDI recorders join the shortcut rows, Reset Defaults must ALSO clear the MIDI map, debounce lives in `AppearanceStore.midiDebounceMs`.
- **Step 8 (done):** Core: `Keywords/KeywordResolver.swift` (`KeywordResolver.resolve` → `.existing(id)`/`.create(components)`; single names via `firstMatch` Q16, `" > "` input as exact path so accepted suggestions Q17 resolve to their node; unmatched text created uppercased ad-hoc), `KeywordAutocomplete` (substring filter over `allPaths()` — one tree covers the spec's vocabulary∪index split), `AutocompleteHighlight` (§9.8 arrow table incl. `clamp` for shrinking lists), `KeywordChipBuilder` (`commonKeywordIds` intersection Q14, `chips` ordered group-position→alpha, ungrouped `colorHex=nil`→grey last Q18, colours via `effectiveGroupId` C2). App: `LibraryController+Keywords` — assignments follow the mutation contract (memory-first, async write, `.photosUpdated`); vocabulary edits are sync `writeSync` + tree rebuild and emit `.photosUpdated(carriers)` **only when facts change** (keyword rename C4 / subtree delete / group delete C3); group rename/color/reorder mutate the snapshot only (names & colours aren't facts — chips re-render via Observation). `InspectorView` (§9.8: title, stars with Q12 tap-current-clears + U3 batch + U4 MIXED badge, ADD KEYWORD with dropdown overlay + `.onKeyPress` arrows/Return so nothing leaks to the grid, chips one-per-row, footer Reveal-anchor + `ShareLink`). `SettingsView` (Settings scene, 600×500; only Keywords tab — Appearance/Shortcuts are step 9) + `KeywordsSettingsView` (groups NAME (top-level n), nine-colour palette popover/sheet, drag reorder via shared `RowReorderDelegate` — extracted from SidebarView; keyword outline with rename-alert/add-sub/delete; U7 alerts with counts, group delete warns about referencing rules; "NEW KEYWORD" created first then rename dialog — cancel keeps it, quirk preserved; sibling-unique names get " 2" suffixes because of the DB unique constraint). Ad-hoc keywords (groupId nil roots) hidden from the editor by design. New DAO: `KeywordDAO.renameGroup/setGroupColor`. New hook `BV_TEST_KEYWORDS=1` (vocabulary create → assign "anna" → Q14/Q18/C4/toggle/persistence, prints BVKEY lines). Verified: 83 core tests green; headless BVKEY run all-pass; visual check (inspector chip colours, settings tree, dropdown + highlight + Return commit, arrows stay out of the grid). Step 9 notes: keyword shortcut toggles should call `toggleKeyword(text:forPhotoIds:)` (C7 normalisation happens naturally in `KeywordResolver`); `SettingsView` TabView is where Appearance/Shortcuts tabs land; `Color(hex:)` in `App/Views/ColorHex.swift` already parses `#RRGGBBAA` (C8).
- **Step 7 (done):** Core: `Query/` — `RuleType`/`RuleOperator`/`QueryEngine` (§7.3 matrix table-driven-tested; unknown *types* skipped per D6, unknown *operators* on known types = false; U10 adds `captureDate` and implements rating/keywordCount `doesNotEqual`), `PhotoQueryFacts` (resolved paths + effective group ids — `KeywordTree.effectiveGroupId` walks to the nearest grouped ancestor, fixing C2 for nested keywords), `SidebarItem` (+string codec, persisted in `libraryMeta.selectedCollection`) and `SidebarFilter` (allPhotos/lastImport with real match-nothing before first import Q8/rating exact Q9/collection), `CollectionCountsStore` (per-photo membership memo → true deltas, O(changed × collections)). App: `LibraryController+Collections` (CRUD with **synchronous** writes — rare structural edits, ids needed immediately; snapshot mirrored in memory; `.collectionsChanged` event), `SidebarViewModel` (counts via store deltas on `.photosUpdated`, collapse state persisted per library), `SidebarView` (spec §9.2 layout incl. selection styling, live drag-reorder via DropDelegate.dropEntered, context menus, U7 delete confirmations, name-prompt alerts), `CollectionEditorSheet` (edits a `CollectionDraft` copy; U10: Unrated star position, Capture Date rule with DatePicker storing unix seconds, hint banner on empty rules; type change resets operator+value). CenterViewModel: `activeItem` replaces the interim unrated filter; collection switch = `selectSidebarItem` → **auto-select first** (§10.4); rule edits on the active collection re-apply via the Q1 path; deleted active collection degrades to ALL PHOTOS. Selected item survives relaunch (verified live). New collections auto-select and show everything (Q6). Verified: 76 core tests green; headless run (BVCULL/BVQUERY): neighbour rule inside a real rules-based UNRATED collection, badge deltas after rating, exact-match star rows, restore after reopen; 50k perf gate intact (select 0.5 ms median, 75 MB). **Testing gotcha for later steps:** killing the app with `kill -9` poisons macOS saved state — next launch shows a modal "reopen windows?" dialog and NO window appears (looks like a hang, `.task` hooks never run). Fix: delete the container's `Saved Application State/...savedState`; `ApplePersistenceIgnoreState` is now set in the container prefs. Also `timeout` does not exist on macOS. Step 8 note: keyword mutations must emit `.photosUpdated` (facts feed counts + filters) and `.collectionsChanged` is NOT needed for vocabulary edits unless group membership changes affect rules — a keyword rename changes paths, so emit `.catalogReplaced` or a targeted photos update for all carriers.
- **Step 6 (done):** Core additions: `AppAction`/`ActionCommand` (action-string grammar), `KeyChord`/`KeyMap` (strict fixed-order key strings, 1:1 assign rules — editor UI is step 9), `NeighbourRule.nextAnchor(for:previousOrder:newOrder:)`, `StableRandomOrder` (reroll/reconcile, RNG injected), `SortEngine`+`SortOption` (U9 five options, **no Rating sort**; comparators deliberately never depend on mutable culling fields, so the list only changes membership, never order), `ControlSurfaceState`, `CatalogEvent`, `SelectionModel.prune`, `PhotoDAO.setRatings/setOrientations` (per-photo values). App: **CatalogEvent pipeline is live** — `LibraryController.addCatalogObserver`, `mutatePhotos` (memory-first, O(1) by `photoIndexById`) + `updateRatings`/`rotatePhotos` (async single-row write-through), `refreshSnapshot()`/install/close emit `.catalogReplaced`. `CenterViewModel` (replaces GridViewModel) owns viewMode/sort/columns/selection/panel-visibility/visible list (incremental `photosDidChange`: membership check only for changed ids, binary-search insertion, neighbour rule when the anchor drops out) and publishes `controlSurface` (recomputed on anchor/list/view-mode change — step 11 consumes). **Interim filter**: `unratedOnly` + bottom-bar checkbox stand in for collections; step 7 replaces it with the query engine — route filter changes through `applyFilterChange()` to keep Q1. `ShortcutMonitor` = app-wide `NSEvent` local monitor (focus-independent → shortcuts survive mode/collection switches); it passes events through when firstResponder is a text view (Q21 groundwork), for panels, or windows with sheets. **Menu key equivalents only for chords with ⌘/⌃** (PhotoCommands.menuShortcut): bare-key equivalents would fire exactly when the monitor deliberately passes (text input) — modifier-less bindings work via the monitor and just show no shortcut in the menu. View-menu items live in the system View menu (CommandGroup after .sidebar), not a second custom menu. KeyMapStore persists under UserDefaults key `keyboardShortcuts`. Keyword commands parse but no-op until step 8/9 (C7 fix at binding creation). moveUp/moveDown are grid-only for `.keyboard`/`.menu` sources; `.midi` bypasses (step 11). Verified: 62 core tests green; headless BVCULL run (unrated filter: rate3 advances to neighbour at same position, ratingUp reachable, rotate cycles 1→6→3, all persisted across reopen); 50k perf gate intact (select median 0.4 ms, jump 0.5 ms, 73 MB); **rotation sign verified visually** — orientation 6 renders 90° CW (step-5 TODO closed). Q22 top-aligned crop still pending (step 12). New hooks: BV_TEST_CULL/BV_TEST_SINGLE/BV_TEST_ROTATE.
- **Step 1 (done):** XcodeGen 2.46 installed via brew. Ad-hoc signing (`CODE_SIGN_IDENTITY: "-"`), no dev team. Entitlements: sandbox + user-selected read-write + app-scope bookmarks (USB deferred to step 11). Deployment target macOS 14.0. GRDB resolved via SPM in BallastCore.
- **Step 5 visual check (done):** screenshots confirm grid rendering (5 columns, square cells, bottom bar count+slider, three panes), selection border, and minimal scroll-to-anchor. Screen-recording permission for Terminal is granted — `screencapture -x <file>` + Read works for future visual checks (bring the app frontmost via System Events first). Known non-issue: semi-transparent PNGs flatten to white in the JPEG thumbnail cache (JPEG has no alpha) — real photos are opaque; the perf fixtures are 50%-alpha red, which is why fresh decodes and cache hits render different shades.
- **Step 5 (done):** **The perf gate FAILED for SwiftUI LazyVGrid at 50k** (select median 601 ms, jump median 867 ms / max 30 s, 541 MB) — the grid backend is **NSCollectionView** as planned: `PhotoGridView` is an NSViewRepresentable (Coordinator = dataSource, `isSelectable=false`, clicks handled in `GridItemView.mouseDown` with modifier flags → `GridViewModel.handleClick`), `GridFlowLayout` recomputes square cells in `prepare()`, items are `GridViewItem` with CALayer-contents display and per-item load Task cancelled in `prepareForReuse`. **Results at 50k: select median 0.1 ms, jump 0.3 ms, 51 MB footprint; second run 78 disk hits / 2 decodes.** `ThumbnailPipeline` (actor): buckets 256/768/2048, decode unrotated (Q5), NSCache 300 MB + JPEG disk cache in Caches/Thumbnails/<libraryUUID> keyed sha256(path|mtime|bucket) — JPEG not HEIC (HEVC is unreliable for tiny images). Bounded 8 decode slots via continuation queue; decode work in `nonisolated static async` funcs (off-actor). CGImage crosses isolation in `CGImageBox` (@unchecked Sendable). Known TODOs for later steps: Q22 top-aligned crop is currently center-crop (contentsGravity) — refine in step 12; rotation transform sign must be verified visually with real photos when step 6's rotate action lands; scroll-to-anchor is non-animated minimal-scroll (deliberate deviation from spec's animated scroll). Perf fixtures kept for reuse: 50k PNGs in container tmp `Perf50k/`, library `PerfLib.ballastlib` (closed, still in recents). PerfProbe (BV_TEST_PERF=1 + BV_TEST_QUIT=1) prints BVPERF select/jump/thumbs/footprint lines. Step 6 note: double-click hook is in `GridItemView.mouseDown` (clickCount), currently single-click only.
- **Step 4 (done):** `FolderScanner`/`MetadataReader`/`ImportDAO` live in BallastCore (ImageIO import is fine there — "BallastCore" pragmatically means *swift-test-able*, not Apple-free). MetadataReader: XMP first via `CGImageSourceCopyMetadataAtIndex` + `CGImageMetadataCopyTagWithPath("xmp:Rating"/"dc:subject")` — standard prefixes work without namespace registration, and tests generate XMP fixtures with `CGImageMetadataSetValueWithPath` + `CGImageDestinationAddImageAndMetadata`. D4 is fixed *structurally*: photos belong to folders via `folderId`, removal is FK cascade, no path-prefix matching anywhere. Q7 by construction: no new photos → no batch row → `lastImportBatchId` untouched. Import pipeline: register folder → scan detached → prefilter existing paths (skips metadata reads on rescan) → bounded TaskGroup (12) → one write tx. U1 complete: New Library panel flows into Add Folder; folder drop with no library open creates one first (`handleDropWithoutLibrary`). Remove Folder confirms with photo count (U7). New test hooks: `BV_TEST_IMPORT=<abs path>`, `BV_TEST_RESCAN=1`, `BV_TEST_NONRECURSIVE=1`, `BV_TEST_REMOVE=<abs path>`. Note for step 7: LAST IMPORT reads `snapshot.meta.lastImportBatchId`. Note for step 5: `refreshSnapshot()` is a full reload after import — fine for now, replaced by CatalogEvents in step 6/7.
- **Step 3 (done):** `LibraryController` (@MainActor @Observable, injected via `.environment`) owns library/snapshot/recents/errorMessage; `BookmarkStore` persists security-scoped bookmarks (keys `lastOpenedLibraryBookmark`, `recentLibraryBookmarks`; recents stored as raw bookmark blobs — never re-create other entries' bookmarks, that needs live access). UTType `com.bolliboll.ballastviewer.library` declared in project.yml `info:` block; Info.plist is now GENERATED by xcodegen at `App/Resources/Info.plist` and gitignored. **Headless testing:** debug-only `TestHooks` (App/Services/TestHooks.swift) — run the app binary with env `BV_TEST=1` plus `BV_TEST_CREATE=<name>` / `BV_TEST_OPEN=<name>` / `BV_TEST_CLOSE=1` / `BV_TEST_QUIT=1`; it prints `BVTEST opened=… recents=… photos=… error=…`. Test libraries live in the sandbox container tmp (`~/Library/Containers/com.bolliboll.ballastviewer/Data/tmp`). Extend these hooks in later steps (e.g. import) instead of inventing new verification paths. U1 partially done: onboarding buttons exist; "New Library flows into Add Folder" + folder-drop need import and move to step 4.
- **Step 2 (done):** Schema v1 in `LibrarySchema.swift` with camelCase columns matching record properties (no CodingKeys). Dates stored via GRDB default (datetime text), not REAL epoch. `photo.filename` is a plain column set in `PhotoRecord.init` from the path. Keyword uniqueness: `UNIQUE(parentId, name)` + partial unique index for top level (SQLite NULLs are distinct). GRDB gotchas learned: referenced tables must be created BEFORE referents inside a migration (GRDB resolves the referenced PK at DDL time), and `t.belongsTo("smartGroup")` names the column `smartGroupId` — use explicit `t.column(...).references(...)` when the record property is named differently. Measured: 50k insert 1.7 s (one write tx), snapshot load 0.24 s. API surface for step 3+: `LibraryDatabase.create/open(at:)`, `LibrarySnapshot.load(_:)`, `PhotoDAO`/`KeywordDAO`/`CollectionDAO` (all take a `Database`, callers wrap in `pool.write`), `KeywordTree` (rebuild after vocabulary mutations), `KeywordDAO.ensurePath(_:groupId:in:)` for find-or-create.
